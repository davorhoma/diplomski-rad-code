using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum DieValue
{
    NONE = 0,
    JOKER = 1,
    HAT = 2,
    MASK = 3,
    BOOT = 4,
    HEEL = 4, // Temporary alias for BOOT. Images are named with 'Heel' but symbol should be called 'Boot'
    GUN = 5,
    HORSESHOE = 6
};
